import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#F9FAFB',
  },
  createRoomView: {
    margin: 16,
    position: 'absolute',
    bottom: 0,
    right: 0,
  },
});
